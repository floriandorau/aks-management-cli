#!/usr/bin/env node
require('yargs/yargs')(process.argv.slice(2))
    .commandDir('../cmds')
    .demandCommand()
    .version()
    .completion()
    .options({
        'debug': {
            alias: 'd',
            describe: 'Debug mode',

        }
    })
    .help('help')
    .argv;